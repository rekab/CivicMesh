#pragma once
// Empty stub. Adafruit_GFX.h includes this unconditionally but the
// Adafruit_GFX class itself never references Adafruit_I2CDevice — that
// symbol is used by Adafruit_SPITFT (which we do not vendor).
